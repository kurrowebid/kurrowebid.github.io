import GetResponseTab from './GetResponseTab.vue';

const { __ } = wp.i18n;

const title = __( 'GetResponse API', 'madx-form-builder' );
const component = GetResponseTab;

export {
	title,
	component
}